# meta-ads-pro v1.1 — Upgrade Operação Ahoy — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar os gaps do plugin meta-ads-pro pro funil real da operação Ahoy/CliniAmp: pipeline de lead completo (form → GHL nativa + webhook VPS → CAPI), boost de post orgânico do IG, criativo avançado (crops, asset customization, carrossel, preview oficial), completude de públicos/pixel e upgrade de versão da API.

**Architecture:** O plugin segue sendo prosa executável (SKILL.md com payloads + libs bash + scripts Python stdlib). Features novas entram no mesmo idioma: payloads em SKILL.md, helpers em `lib/`, testes bash em `tests/NN-*.sh`. A única peça nova de runtime é o `webhook-receiver/` (FastAPI, deployado na VPS Hermes atrás do Cloudflare Tunnel `webhooks.ahoy.digital`).

**Tech Stack:** bash 3.2+, jq, curl, Python 3 stdlib (plugin) · Python 3.11 + FastAPI + uvicorn (receiver, só na VPS) · Graph API / LeadConnector API v2 (GHL).

## Global Constraints

- **Anti-fabricação:** payloads de features novas foram escritos com base na doc conhecida da Marketing API. Os spikes (Tasks 2–4) são **autoritativos**: se um spike contradisser um payload de task posterior, corrija a task ANTES de executá-la e anote no doc do spike.
- Repo: `/Users/flavioahoy/Documents/projects/meta-ads-pro`. Raiz do plugin: `plugins/meta-ads-pro/` (abreviado como `PLUGIN/` neste doc).
- Toda chamada HTTP à Meta passa por `PLUGIN/lib/graph_api.sh::graph_api` — nunca `curl` direto (exceção existente: `debug_token`/headers em `preflight.sh`).
- Tudo criado na Meta nasce `status: PAUSED`. Nunca ecoar `$META_ACCESS_TOKEN`.
- Testes live usam prefixo `TEST_` no nome + cleanup via trap EXIT (idioma de `tests/08-lead-forms.sh:27-36`).
- Valores user-controlled entram em payload via `jq --arg`, nunca heredoc.
- bash 3.2 compatível (macOS); scripts Python do plugin são stdlib-only (sem pip). O receiver é a exceção deliberada (roda na VPS, tem `requirements.txt` próprio).
- PT-BR com acentos corretos em toda doc/UX.
- Commits frequentes, um por task no mínimo. Branch de trabalho: `feat/v1.1-operacao-ahoy` (tracks paralelos: worktrees `feat/v1.1-track-a|b|c` a partir dela).
- Validação live usa a infra Ahoy (conta `act_` da Ahoy, página + IG da Ahoy, subconta GHL de teste). Nada roda em conta de cliente até a fase final de piloto.
- Env vars novas ficam no `.env` do projeto do cliente (modelo um-projeto-por-cliente), documentadas no setup (Task 22).

## Ordem de execução

```
F0 (sequencial):  Task 1 → Tasks 2,3,4 (podem rodar em paralelo entre si)
                          ↓
Track A (Tasks 5–12)   Track B (Tasks 13–16)   Track C (Tasks 17–21)   ← paralelos (worktrees)
                          ↓ (merge dos 3 tracks)
F-final (sequencial): Task 22 → Task 23 → Task 24
```

## Estrutura de arquivos (visão geral)

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `PLUGIN/lib/graph_api.sh` | modificar | bump default `META_API_VERSION` |
| `PLUGIN/lib/preflight.sh` | modificar | des-hardcodear v25.0; novos checks (GHL, CAPI, subscrição leadgen, receiver) |
| `PLUGIN/lib/utm.sh` | modificar | `build_tracking_parameters` |
| `PLUGIN/flows/lead-forms/SKILL.md` | modificar | tracking_parameters, thank you completo, condicionais, test_leads |
| `PLUGIN/flows/crm/SKILL.md` + `PLUGIN/commands/meta-ads-crm.md` | criar | integração GHL nativa + CAPI do funil |
| `PLUGIN/flows/boost/SKILL.md` + `PLUGIN/commands/meta-ads-boost.md` | criar | boost de post/Reel próprio do IG |
| `PLUGIN/flows/anuncios/SKILL.md` | modificar | generatepreviews, image_crops, asset_customization_rules, carrossel |
| `PLUGIN/flows/publicos/SKILL.md` | modificar | exclusões, saved audiences write, pixel create |
| `PLUGIN/flows/conjuntos/SKILL.md` | modificar | exclusões no targeting |
| `PLUGIN/flows/news/SKILL.md` + `PLUGIN/commands/meta-ads-news.md` | criar | changelog da Meta sob demanda |
| `PLUGIN/flows/setup/SKILL.md` | modificar | novos passos (GHL, dataset CAPI, receiver) |
| `webhook-receiver/` (raiz do repo) | criar | receiver FastAPI leadgen → GHL |
| `docs/spikes/*.md` | criar | achados dos spikes (fonte de verdade) |
| `PLUGIN/tests/08-lead-forms.sh` | modificar | testes 10–12 reais + tracking_parameters |
| `PLUGIN/tests/18-boost-ig.sh`, `19-crm-capi.sh`, `20-criativo-avancado.sh` | criar | suites novas |

---

## F0 — FUNDAÇÃO (sequencial, pré-requisito de tudo)

### Task 1: Upgrade de versão da Marketing API (parametrizado + auditoria)

**Files:**
- Modify: `PLUGIN/lib/graph_api.sh:9`
- Modify: `PLUGIN/lib/preflight.sh:28,49,116` (des-hardcodear)
- Modify: `PLUGIN/lib/upload_media.sh:97,144`, `PLUGIN/lib/upload_video.sh:27`, `PLUGIN/lib/error-resolver.sh:194,204`, `PLUGIN/lib/rollback.sh:85`
- Modify: `PLUGIN/lib/_py/import_existing.py:41`
- Modify: `PLUGIN/flows/setup/SKILL.md:89` (linha `META_API_VERSION=v25.0` do heredoc)
- Create: `docs/spikes/2026-07-api-version.md`

**Interfaces:**
- Produces: `docs/spikes/2026-07-api-version.md` com a versão-alvo `VNEW` (ex.: `v27.0`) e lista de breaking changes. Todas as tasks seguintes usam `VNEW` onde este plano escreve `v25.0`.

- [ ] **Step 1: Descobrir a versão atual da Graph API.** Buscar `https://developers.facebook.com/docs/graph-api/changelog` (WebFetch ou browser). Anotar a versão mais recente disponível (`VNEW`) e a data de depreciação da v25.0.

- [ ] **Step 2: Auditar breaking changes.** Para cada versão entre v25.0 e `VNEW`, ler a página de changelog correspondente e cruzar com os recursos que o plugin usa (grep no repo): `leadgen_forms`, `adcreatives`, `object_story_spec`, `asset_feed_spec`, `customaudiences`, `adrules_library`, `insights`, `adspixels`, `search?type=adinterest|adgeolocation`, `promote_pages`, `connected_instagram_accounts`, `instagram_user_id`/`instagram_actor_id`, `subscribed_apps`. Escrever `docs/spikes/2026-07-api-version.md`:

```markdown
# Spike: upgrade Marketing API v25.0 → VNEW (2026-07)

## Versão-alvo
VNEW = vXX.Y (mais recente em YYYY-MM-DD; v25.0 depreca em YYYY-MM-DD)

## Breaking changes que afetam o plugin
| Mudança | Versão | Arquivo afetado | Ação |
|---|---|---|---|
| (preencher com achados reais; se nenhum: "nenhum breaking change afeta os endpoints usados") |

## Campos renomeados/novos relevantes pra v1.1
- instagram_actor_id vs instagram_user_id em adcreatives: (estado real na VNEW)
- (outros achados)
```

- [ ] **Step 3: Trocar o default e des-hardcodear.** Em `graph_api.sh:9`: `API_VERSION="${META_API_VERSION:-VNEW}"`. Em `preflight.sh`, substituir as 3 URLs hardcoded `https://graph.facebook.com/v25.0/...` por `https://graph.facebook.com/${META_API_VERSION:-VNEW}/...`. Mesmo padrão nos demais arquivos listados (upload_media, upload_video, error-resolver, rollback, import_existing.py — neste último, `DEFAULT_API_VERSION = "VNEW"`). Atualizar `flows/setup/SKILL.md:89` e as menções "v25.0" descritivas em `flows/anuncios/SKILL.md:41,68,320` para "VNEW".

- [ ] **Step 4: Rodar a suíte para regressão.** `cd PLUGIN && bash tests/run_all.sh` (com `META_ACCESS_TOKEN`/`PAGE_ID`/`AD_ACCOUNT_ID` do projeto de teste Ahoy exportados). Expected: mesmas contagens de PASS da baseline (rodar a baseline ANTES do bump para comparar). Qualquer FAIL novo = breaking change não mapeado → volta ao Step 2.

- [ ] **Step 5: Commit.**

```bash
git add PLUGIN/lib/graph_api.sh PLUGIN/lib/preflight.sh PLUGIN/lib/upload_media.sh \
  PLUGIN/lib/upload_video.sh PLUGIN/lib/error-resolver.sh PLUGIN/lib/rollback.sh \
  PLUGIN/lib/_py/import_existing.py PLUGIN/flows/setup/SKILL.md PLUGIN/flows/anuncios/SKILL.md \
  docs/spikes/2026-07-api-version.md
git commit -m "feat: upgrade Marketing API v25.0 -> VNEW, versão parametrizada em todos os pontos"
```

### Task 2: Spike live — lead form avançado (condicionais, tracking_parameters, thank you CTAs)

**Files:**
- Create: `docs/spikes/2026-07-leadform-avancado.md`

**Interfaces:**
- Consumes: `graph_api` (`PLUGIN/lib/graph_api.sh`), env do projeto Ahoy (`META_ACCESS_TOKEN`, `PAGE_ID` + page token).
- Produces: doc com veredito EXISTE/NÃO EXISTE + payload exato que funcionou para: (a) dropdowns encadeados, (b) branching de pergunta, (c) `tracking_parameters`, (d) lista completa de `button_type` aceitos em `thank_you_page`. As Tasks 6–8 dependem deste doc.

- [ ] **Step 1: Testar `tracking_parameters` na criação.** Com o payload mínimo de `tests/08-lead-forms.sh::build_minimal_form_payload` + campo extra:

```bash
source PLUGIN/lib/graph_api.sh
# NÃO fazer source de tests/08-lead-forms.sh (executaria a suíte). Payload mínimo inline:
payload=$(jq -nc --arg name "TEST_SPIKE_$(date +%s)" \
  --arg privacy_url "https://lp.ahoy.digital/politicas-privacidade" '{
  name:$name,
  questions:[{type:"FULL_NAME"},{type:"EMAIL"},{type:"PHONE"}],
  privacy_policy:{url:$privacy_url},
  context_card:{title:"Intro spike", content:["Descrição de introdução"]},
  thank_you_page:{title:"Obrigado!", body:"Em contato em breve"},
  disqualified_thank_you_page:{title:"Não elegível", body:"Siga nosso IG"},
  tracking_parameters:{utm_source:"meta-leadform", utm_campaign:"spike-2026-07"}
}')
graph_api POST "${PAGE_ID}/leadgen_forms" "$payload"
```

Se a API rejeitar o formato objeto, tentar string JSON (`tracking_parameters: "{\"utm_source\":...}"`) e query-string. Registrar o formato aceito. Depois `GET /{form_id}?fields=tracking_parameters` para confirmar round-trip.

- [ ] **Step 2: Gerar lead de teste e verificar o que volta.** `graph_api POST "${form_id}/test_leads" '{}'` (se o endpoint não existir na VNEW, usar a Lead Ads Testing Tool em https://developers.facebook.com/tools/lead-ads-testing e anotar isso). Então `graph_api GET "${form_id}/leads?fields=id,created_time,field_data,ad_id,campaign_id"` e registrar: os tracking_parameters vêm no lead? Em qual campo?

- [ ] **Step 3: Testar dropdowns encadeados.** Tentar POST de form com pergunta CUSTOM de dependência (payload candidato abaixo; ajustar conforme erro da API — o objetivo do spike é achar a forma real ou provar que não existe):

```json
{"type":"CUSTOM","key":"procedimento","label":"Qual procedimento?","input_type":"MULTIPLE_CHOICE",
 "options":[{"value":"Estética","key":"est"},{"value":"Ortodontia","key":"orto"}],
 "dependent_conditional_questions":[
   {"name":"tipo_estetica","field_key":"tipo_est","input_type":"MULTIPLE_CHOICE"}]}
```

Registrar: campo aceito? erro retornado? Consultar também a doc oficial de `leadgen_forms` questions na VNEW antes de concluir NÃO EXISTE.

- [ ] **Step 4: Testar branching (mostrar/ocultar pergunta).** Mesma abordagem: procurar na doc da VNEW qualquer campo de visibilidade condicional de pergunta; tentar payload; registrar veredito. Se NÃO EXISTE: escrever o plano B no doc (qualificação via dropdown + disqualified_thank_you_page, que já existe).

- [ ] **Step 5: Enumerar button_types da thank you page.** Criar forms de teste (prefixo `TEST_SPIKE_`) variando `thank_you_page.button_type` com os candidatos: `VIEW_URL`, `CALL_BUSINESS`/`CALL`, `DOWNLOAD`, `MESSAGE_BUSINESS`/`MESSAGE`, `SCHEDULE_APPOINTMENT`, `VIEW_ON_FACEBOOK`, `NONE`. Registrar quais a VNEW aceita e os campos obrigatórios de cada (ex.: `phone_number`+`country_code` pra CALL; `website_url` pra VIEW_URL; número WhatsApp pra MESSAGE). Deletar todos os forms de teste no final (`graph_api DELETE`).

- [ ] **Step 6: Escrever `docs/spikes/2026-07-leadform-avancado.md`** com os 4 vereditos + payloads exatos que funcionaram (copiar/colar da sessão) + IDs de teste deletados. Commit:

```bash
git add docs/spikes/2026-07-leadform-avancado.md
git commit -m "docs: spike live lead form avançado (condicionais, tracking_parameters, thank you CTAs)"
```

### Task 3: Spike live — boost de post orgânico do IG

**Files:**
- Create: `docs/spikes/2026-07-boost-ig.md`

**Interfaces:**
- Consumes: env Ahoy (`PAGE_ID`, `INSTAGRAM_USER_ID`, `AD_ACCOUNT_ID`), IG da Ahoy com ≥1 post e ≥1 Reel publicados.
- Produces: doc com o payload exato de creative que funcionou + regras de elegibilidade. Task 13 depende dele.

- [ ] **Step 1: Listar mídia do IG.**

```bash
graph_api GET "${PAGE_ID}?fields=instagram_business_account"   # confirma o IG_USER_ID
graph_api GET "${INSTAGRAM_USER_ID}/media?fields=id,caption,media_type,media_product_type,permalink,timestamp,like_count,comments_count&limit=10"
```

Registrar: os campos retornam? `media_product_type` distingue `FEED`/`REELS`?

- [ ] **Step 2: Criar creative a partir de um post existente.** Payload candidato (ajustar conforme doc VNEW — em versões recentes `instagram_actor_id` virou `instagram_user_id`):

```bash
payload=$(jq -nc --arg pid "$PAGE_ID" --arg ig "$INSTAGRAM_USER_ID" --arg media "<IG_MEDIA_ID>" '{
  name: "TEST_SPIKE_boost_ig",
  object_story_spec: {page_id: $pid, instagram_user_id: $ig},
  source_instagram_media_id: $media
}')
graph_api POST "${AD_ACCOUNT_ID}/adcreatives" "$payload"
```

Registrar o payload exato aceito. Em erro, testar variações documentadas na VNEW (`object_id`, `instagram_actor_id`).

- [ ] **Step 3: Subir a cadeia até o ad (PAUSED).** Criar campanha `TEST_SPIKE_` (ENGAGEMENT), adset mínimo, ad com o creative do Step 2. Confirmar no Ads Manager que o anúncio referencia o post orgânico (comentários/curtidas preservados — abrir o preview). Registrar print/URL do preview.

- [ ] **Step 4: Testar um Reel com música.** Repetir Step 2 com um Reel que usa áudio de biblioteca. Registrar o erro de elegibilidade (ou sucesso). Isso vira a mensagem de erro amigável da Task 13.

- [ ] **Step 5: Rollback + doc + commit.** Deletar ad/adset/campanha/creative de teste. Escrever `docs/spikes/2026-07-boost-ig.md` com payload final, campos de listagem e regras de elegibilidade. `git commit -m "docs: spike live boost de post orgânico IG"`.

### Task 4: Spike — requisitos do webhook de leadgen (app, permissões, subscrição)

**Files:**
- Create: `docs/spikes/2026-07-webhook-leadgen.md`

**Interfaces:**
- Produces: doc com: modo do app (dev/live), necessidade ou não de Advanced Access pra `leads_retrieval`, confirmação de que `POST /{page_id}/subscribed_apps` funciona com o page token, e o par (verify token, app secret) definido pro receiver. Tasks 9–11 dependem dele.

- [ ] **Step 1: Identificar o app.** `graph_api GET "app?fields=id,name"` (o token System User é emitido por um app). Anotar `APP_ID`. No painel https://developers.facebook.com/apps/{APP_ID}: anotar modo (dev/live) e nível de acesso de `leads_retrieval` e `pages_manage_ads` (Standard vs Advanced).

- [ ] **Step 2: Testar subscrição da página.** Com page token (obtido via `graph_api GET "${PAGE_ID}?fields=access_token"` — nunca ecoar):

```bash
# usa curl direto porque o page token ≠ META_ACCESS_TOKEN (exceção documentada)
curl -sS -X POST "https://graph.facebook.com/VNEW/${PAGE_ID}/subscribed_apps" \
  -d "subscribed_fields=leadgen" -d "access_token=${PAGE_TOKEN}"
# verificar: GET /{PAGE_ID}/subscribed_apps → app listado com leadgen
```

- [ ] **Step 3: Documentar a config de webhook do app.** No painel do app → Webhooks → Page: registrar que a URL de callback será `https://webhooks.ahoy.digital/meta-leads` e o verify token será gerado na Task 10 (`openssl rand -hex 24`). NÃO configurar ainda (o receiver não existe) — só validar que a tela aceita a URL e anotar o App Secret (vai pro `.env` do receiver, nunca pro repo).

- [ ] **Step 4: Escrever `docs/spikes/2026-07-webhook-leadgen.md`** com: APP_ID, modo, acessos, resultado do subscribed_apps, e o checklist exato de configuração que a Task 11 vai executar. `git commit -m "docs: spike requisitos webhook leadgen (app, permissões, subscrição)"`.

---

## TRACK A — PIPELINE DE LEAD (worktree `feat/v1.1-track-a`)

### Task 5: `lib/utm.sh` — builder de tracking_parameters

**Files:**
- Modify: `PLUGIN/lib/utm.sh` (append no final, após `is_external_url`)
- Modify: `PLUGIN/tests/08-lead-forms.sh` (novo teste client-side)

**Interfaces:**
- Produces: `build_tracking_parameters <form_name> [extra_json]` → ecoa JSON `{"utm_source":"meta-leadform","utm_medium":"trafego-pago","utm_campaign":"{YYYYMMDD}_{form-slug}"}` (mergeado com `extra_json` se dado). Task 6 consome. Se o spike T2 concluiu que a API só aceita string, o caller (Task 6) serializa — esta função sempre retorna objeto JSON.

- [ ] **Step 1: Escrever o teste (client-side, sem token).** Em `PLUGIN/tests/08-lead-forms.sh`, adicionar antes da seção "Execução" e registrar na lista de execução após `test_12_*`:

```bash
# ─── Test 13: build_tracking_parameters gera JSON válido ──────────────────────
test_13_build_tracking_parameters() {
  # shellcheck source=../lib/utm.sh disable=SC1091
  source "$PLUGIN_ROOT/lib/utm.sh"
  local out
  out=$(build_tracking_parameters "Form Clínica São João") \
    || _fail "test_13_build_tracking_parameters" "função falhou"
  echo "$out" | jq -e '.utm_source == "meta-leadform" and .utm_medium == "trafego-pago"' >/dev/null \
    || _fail "test_13_build_tracking_parameters" "campos base errados: $out"
  echo "$out" | jq -re '.utm_campaign' | grep -Eq '^[0-9]{8}_form-clinica-sao-joao$' \
    || _fail "test_13_build_tracking_parameters" "utm_campaign fora do padrão: $out"
  # merge de extras
  out=$(build_tracking_parameters "X" '{"utm_content":"boost"}')
  echo "$out" | jq -e '.utm_content == "boost"' >/dev/null \
    || _fail "test_13_build_tracking_parameters" "merge de extras falhou: $out"
  _pass "test_13_build_tracking_parameters"
}
```

- [ ] **Step 2: Rodar e ver falhar.** `bash PLUGIN/tests/08-lead-forms.sh` → Expected: `✗ test_13...` (função não existe).

- [ ] **Step 3: Implementar em `PLUGIN/lib/utm.sh`** (append):

```bash
# build_tracking_parameters <form_name> [extra_json]
# JSON de tracking pro campo tracking_parameters do lead form (volta grudado
# em cada lead). Formato exato aceito pela API: ver docs/spikes/2026-07-leadform-avancado.md.
build_tracking_parameters() {
  local form_name="$1"
  local extra="${2:-{\}}"
  local slug today
  slug=$(slugify "$form_name")
  today=$(date +%Y%m%d)
  jq -nc --arg c "${today}_${slug}" --argjson extra "$extra" \
    '{utm_source:"meta-leadform", utm_medium:"trafego-pago", utm_campaign:$c} + $extra'
}
```

- [ ] **Step 4: Rodar e ver passar.** `bash PLUGIN/tests/08-lead-forms.sh` → Expected: `✓ test_13_build_tracking_parameters`.

- [ ] **Step 5: Commit.** `git add PLUGIN/lib/utm.sh PLUGIN/tests/08-lead-forms.sh && git commit -m "feat(lead-forms): builder de tracking_parameters em lib/utm.sh"`

### Task 6: Lead form — `tracking_parameters` no fluxo de criação

**Files:**
- Modify: `PLUGIN/flows/lead-forms/SKILL.md` (novo passo entre 8 e 9; payload final do passo 9 em `:266-277`; regra 8 em `:333`)
- Modify: `PLUGIN/tests/08-lead-forms.sh` (teste live)

**Interfaces:**
- Consumes: `build_tracking_parameters` (Task 5); formato aceito documentado no spike T2.

- [ ] **Step 1: Teste live primeiro.** Adicionar a `tests/08-lead-forms.sh` (na lista de execução, após test_13):

```bash
# ─── Test 14: form criado com tracking_parameters (live) ──────────────────────
test_14_tracking_parameters_roundtrip() {
  if ! _need_token; then
    _skip "test_14_tracking_parameters_roundtrip" "sem META_ACCESS_TOKEN/PAGE_ID"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"; source "$PLUGIN_ROOT/lib/utm.sh"
  local tp payload response fid
  tp=$(build_tracking_parameters "TEST tracking")
  # ajustar a linha abaixo pro formato aceito no spike T2 (objeto vs string):
  payload=$(build_minimal_form_payload | jq --argjson tp "$tp" '. + {tracking_parameters:$tp}')
  response=$(graph_api POST "${PAGE_ID}/leadgen_forms" "$payload") \
    || _fail "test_14_tracking_parameters_roundtrip" "POST falhou: $response"
  fid=$(echo "$response" | jq -r '.id // empty')
  created_forms+=("$fid")
  graph_api GET "${fid}?fields=id" >/dev/null \
    || _fail "test_14_tracking_parameters_roundtrip" "GET pós-criação falhou"
  _pass "test_14_tracking_parameters_roundtrip (fid=$fid)"
}
```

Rodar → Expected: FAIL ou comportamento a confirmar (este teste valida o formato do spike; se o spike disse "string", trocar `--argjson` por `--arg tp "$(echo "$tp" | jq -c .)"`).

- [ ] **Step 2: Editar o SKILL.md.** Inserir novo passo após o Passo 8 (linha ~256):

```markdown
### Passo 8.5 — Tracking parameters (OBRIGATÓRIO)

Todo form leva `tracking_parameters` — key-values que a Meta devolve grudados em
CADA lead (via API/webhook; conferir no GHL o que a integração nativa mapeia).

\```bash
source "$CLAUDE_PLUGIN_ROOT/lib/utm.sh"
tp=$(build_tracking_parameters "$form_name")
payload=$(echo "$payload" | jq --argjson tp "$tp" '. + {tracking_parameters:$tp}')
\```

Formato validado no spike `docs/spikes/2026-07-leadform-avancado.md`. Mostrar os
valores ao usuário no resumo pré-POST.
```

Atualizar o payload final do Passo 9 (`:266-277`) adicionando `"tracking_parameters": {...}` ao exemplo, e a regra inviolável 8 (`:333`) mencionando que tracking_parameters é obrigatório junto do UTM estático.

- [ ] **Step 3: Rodar testes.** `bash PLUGIN/tests/08-lead-forms.sh` → Expected: test_14 PASS.

- [ ] **Step 4: Commit.** `git add PLUGIN/flows/lead-forms/SKILL.md PLUGIN/tests/08-lead-forms.sh && git commit -m "feat(lead-forms): tracking_parameters obrigatório no fluxo de criação"`

### Task 7: Lead form — thank you page com leque completo de CTAs

**Files:**
- Modify: `PLUGIN/flows/lead-forms/SKILL.md:172-216` (Passo 7) e `:218-245` (Passo 8)

**Interfaces:**
- Consumes: lista de `button_type` validada no spike T2 (Step 5). O bloco abaixo assume o conjunto candidato — **substituir pelo conjunto real do spike**.

- [ ] **Step 1: Reescrever o menu de CTA do Passo 7** com todos os tipos aceitos na VNEW e seus campos obrigatórios:

```markdown
CTA button (tipos validados na VNEW — spike 2026-07-leadform-avancado.md):
  [1] Visitar site (VIEW_URL)            — requer website_url (recebe UTM estático)
  [2] Ligar agora (CALL)                 — requer country_code + phone_number
  [3] Baixar arquivo (DOWNLOAD)          — requer website_url de PDF (recebe UTM estático)
  [4] WhatsApp (MESSAGE)                 — requer número WhatsApp Business da página
  [5] Agendar horário (SCHEDULE_APPOINTMENT, se aceito na VNEW) — requer URL de agenda
  [6] Ver no Facebook (VIEW_ON_FACEBOOK, se aceito)
  [N] Sem botão

Payload por tipo (exemplos completos):
  VIEW_URL:  {"button_type":"VIEW_URL","button_text":"Acessar","website_url":"https://...?utm_..."}
  CALL:      {"button_type":"CALL","button_text":"Ligar agora","country_code":"BR","phone_number":"5591..."}
  MESSAGE:   {"button_type":"MESSAGE","button_text":"Chamar no WhatsApp"}
```

Mantendo as regras existentes: UTM estático em toda `website_url` (Passo 7, `:186-202`), thank you desqualificado obrigatório com CTA distinto (Passo 8).

- [ ] **Step 2: Validar live.** Criar 1 form `TEST_` por tipo de CTA novo que o spike aprovou (reusar o padrão do test_14; pode ser manualmente na sessão, com DELETE ao final). Expected: todos os POSTs 200.

- [ ] **Step 3: Commit.** `git add PLUGIN/flows/lead-forms/SKILL.md && git commit -m "feat(lead-forms): leque completo de CTAs na thank you page (qualificada e desqualificada)"`

### Task 8: Lead form — condicionais reais + testes 10–12 implementados

**Files:**
- Modify: `PLUGIN/flows/lead-forms/SKILL.md:89-137` (Passo 5)
- Modify: `PLUGIN/tests/08-lead-forms.sh:215-226` (substituir stubs)

**Interfaces:**
- Consumes: vereditos (a)/(b) do spike T2. Dois cenários por item: EXISTE → implementar payload real; NÃO EXISTE → documentar limitação + plano B no SKILL.md (sem prosa aspiracional — remover a promessa de `[c] conditional` de `:97,112-116` e do frontmatter `:3` se for o caso).

- [ ] **Step 1: Substituir os stubs por testes reais.** `test_10_qualifier_disqualifier`: POST live de form com pergunta MULTIPLE_CHOICE + `disqualified_thank_you_page`, verificando aceitação (qualifier é client-side na Meta — o teste valida que o form com opções desqualificantes + thank you dupla cria OK). `test_11_conditional_logic`: se spike diz EXISTE, POST com o payload real do spike e assert de round-trip via `GET /{fid}?fields=questions`; se NÃO EXISTE, o teste vira guard-rail:

```bash
test_11_conditional_logic() {
  # Spike 2026-07: API pública NÃO expõe branching (se mudou, atualize o spike doc e este teste)
  if ! _need_token; then _skip "test_11_conditional_logic" "sem token"; return 0; fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local payload response
  payload=$(build_minimal_form_payload | jq \
    '.questions += [{type:"CUSTOM",key:"q_cond",label:"Cond?",input_type:"MULTIPLE_CHOICE",
      options:[{value:"A"},{value:"B"}],
      dependent_conditional_questions:[{name:"dep",field_key:"dep"}]}]')
  if response=$(GRAPH_API_SKIP_RESOLVER=1 graph_api POST "${PAGE_ID}/leadgen_forms" "$payload" 2>&1); then
    created_forms+=("$(echo "$response" | jq -r .id)")
    _fail "test_11_conditional_logic" "API passou a aceitar conditional — atualizar SKILL.md e spike!"
  fi
  _pass "test_11_conditional_logic (limitação confirmada, plano B documentado)"
}
```

`test_12_preview_html`: gerar preview do form via `lib/_py/preview_html.py` com payload mínimo e assert de arquivo HTML não-vazio contendo o título do form.

- [ ] **Step 2: Rodar, ver os 3 falharem/skiparem, implementar o SKILL.md.** Passo 5 ganha a seção final real (dropdowns encadeados com o payload do spike, OU o bloco de limitação):

```markdown
**Condicionais — estado real (spike 2026-07):**
- Qualificar/desqualificar por resposta: SUPORTADO (thank you dupla, Passos 7-8).
- Dropdowns encadeados: <payload real do spike, ou "só via Ads Manager (CSV upload);
  a skill orienta e gera o CSV no formato aceito, mas não cria via API">.
- Branching (mostrar/ocultar pergunta): <idem — payload ou limitação + plano B:
  qualificação por dropdown + telas de obrigado distintas>.
```

- [ ] **Step 3: Rodar suite completa.** `bash PLUGIN/tests/08-lead-forms.sh` → Expected: 0 FAIL, stubs zerados (nenhum SKIP "CP3c").

- [ ] **Step 4: Commit.** `git add PLUGIN/flows/lead-forms/SKILL.md PLUGIN/tests/08-lead-forms.sh && git commit -m "feat(lead-forms): condicionais com estado real da API + testes 10-12 implementados"`

### Task 9: `webhook-receiver/` — app FastAPI (leadgen → GHL)

**Files:**
- Create: `webhook-receiver/app.py`
- Create: `webhook-receiver/config.example.json`
- Create: `webhook-receiver/requirements.txt`
- Create: `webhook-receiver/test_app.py`
- Create: `webhook-receiver/README.md` (visão + variáveis)

**Interfaces:**
- Consumes: App Secret + verify token do spike T4; formato de lead da Graph API (`GET /{leadgen_id}?fields=...`).
- Produces: serviço HTTP com rotas `GET /meta-leads` (verificação), `POST /meta-leads` (eventos), `GET /meta-leads/health`. Config JSON mapeia `page_id → {location_id, ghl_token_env}` (multi-cliente). Task 10 deploya; doctor (Task 21) consome o `/health`.

- [ ] **Step 1: Escrever os testes primeiro** (`webhook-receiver/test_app.py`):

```python
import hashlib, hmac, json, os
os.environ["META_VERIFY_TOKEN"] = "vtoken-test"
os.environ["META_APP_SECRET"] = "secret-test"
os.environ["META_ACCESS_TOKEN"] = "EAAtest"
os.environ["RECEIVER_CONFIG"] = "config.test.json"
os.environ["RECEIVER_STATE_DIR"] = "/tmp/receiver-test"

from fastapi.testclient import TestClient
import app as appmod

client = TestClient(appmod.app)

def _sig(body: bytes) -> str:
    return "sha256=" + hmac.new(b"secret-test", body, hashlib.sha256).hexdigest()

def test_verify_challenge():
    r = client.get("/meta-leads", params={
        "hub.mode": "subscribe", "hub.verify_token": "vtoken-test", "hub.challenge": "42"})
    assert r.status_code == 200 and r.text == "42"

def test_verify_wrong_token_403():
    r = client.get("/meta-leads", params={
        "hub.mode": "subscribe", "hub.verify_token": "errado", "hub.challenge": "42"})
    assert r.status_code == 403

def test_post_bad_signature_403():
    body = json.dumps({"entry": []}).encode()
    r = client.post("/meta-leads", content=body, headers={"X-Hub-Signature-256": "sha256=dead"})
    assert r.status_code == 403

def test_post_leadgen_pushes_to_ghl(monkeypatch):
    pushed = {}
    monkeypatch.setattr(appmod, "fetch_lead", lambda lid: {
        "id": lid, "field_data": [{"name": "full_name", "values": ["Maria"]},
                                  {"name": "email", "values": ["m@x.com"]},
                                  {"name": "phone_number", "values": ["+5591999999999"]}],
        "ad_id": "1", "campaign_id": "2"})
    monkeypatch.setattr(appmod, "push_to_ghl", lambda lead, cfg: pushed.update(lead=lead, cfg=cfg))
    body = json.dumps({"entry": [{"changes": [{"field": "leadgen", "value": {
        "leadgen_id": "L1", "page_id": "PAGE_TEST", "form_id": "F1", "created_time": 1}}]}]}).encode()
    r = client.post("/meta-leads", content=body, headers={"X-Hub-Signature-256": _sig(body)})
    assert r.status_code == 200
    assert pushed["lead"]["id"] == "L1" and pushed["cfg"]["location_id"] == "loc-test"

def test_post_dedup_same_leadgen_id(monkeypatch):
    calls = []
    monkeypatch.setattr(appmod, "fetch_lead", lambda lid: {"id": lid, "field_data": []})
    monkeypatch.setattr(appmod, "push_to_ghl", lambda lead, cfg: calls.append(lead["id"]))
    body = json.dumps({"entry": [{"changes": [{"field": "leadgen", "value": {
        "leadgen_id": "L2", "page_id": "PAGE_TEST", "form_id": "F1", "created_time": 1}}]}]}).encode()
    for _ in range(2):
        client.post("/meta-leads", content=body, headers={"X-Hub-Signature-256": _sig(body)})
    assert calls == ["L2"]

def test_health():
    r = client.get("/meta-leads/health")
    assert r.status_code == 200 and "received_total" in r.json()
```

E `webhook-receiver/config.test.json`: `{"PAGE_TEST": {"location_id": "loc-test", "ghl_token_env": "GHL_TOKEN_TEST"}}` (com `GHL_TOKEN_TEST=x` exportado no conftest ou no topo do test file via `os.environ`).

- [ ] **Step 2: Rodar e ver falhar.** `cd webhook-receiver && python3 -m venv .venv && .venv/bin/pip install fastapi uvicorn httpx pytest && .venv/bin/pytest -q` → Expected: erro de import de `app`.

- [ ] **Step 3: Implementar `app.py`:**

```python
"""Receiver de leadgen da Meta → GoHighLevel. Roda na VPS atrás do Cloudflare Tunnel."""
import hashlib, hmac, json, os, time, urllib.request, urllib.parse
from pathlib import Path
from fastapi import FastAPI, Request, Response, HTTPException

app = FastAPI()
VERIFY_TOKEN = os.environ["META_VERIFY_TOKEN"]
APP_SECRET = os.environ["META_APP_SECRET"].encode()
META_TOKEN = os.environ["META_ACCESS_TOKEN"]
API_VERSION = os.environ.get("META_API_VERSION", "VNEW")
CONFIG = json.loads(Path(os.environ.get("RECEIVER_CONFIG", "config.json")).read_text())
STATE = Path(os.environ.get("RECEIVER_STATE_DIR", "/var/lib/meta-leads")); STATE.mkdir(parents=True, exist_ok=True)
SEEN = STATE / "seen_leadgen_ids.txt"; LOG = STATE / "events.jsonl"

def _log(rec): LOG.open("a").write(json.dumps(rec, ensure_ascii=False) + "\n")

def fetch_lead(leadgen_id: str) -> dict:
    fields = "id,created_time,field_data,ad_id,adset_id,campaign_id,form_id"
    url = (f"https://graph.facebook.com/{API_VERSION}/{leadgen_id}"
           f"?fields={fields}&access_token={urllib.parse.quote(META_TOKEN)}")
    with urllib.request.urlopen(url, timeout=15) as r:
        return json.loads(r.read())

def push_to_ghl(lead: dict, cfg: dict) -> None:
    token = os.environ[cfg["ghl_token_env"]]
    fd = {f["name"]: f["values"][0] for f in lead.get("field_data", []) if f.get("values")}
    body = {
        "locationId": cfg["location_id"],
        "name": fd.get("full_name"), "email": fd.get("email"),
        "phone": fd.get("phone_number"),
        "source": "meta-leadform-webhook",
        "tags": ["meta-lead-ads"],
        # customFields: IDs reais por location, mapeados em cfg["custom_fields"]
        # (ex.: {"utm_campaign": "<CF_ID>"}). Preenchidos na Task 11 Step 3.
        "customFields": [
            {"id": cf_id, "value": str(lead.get(k) or fd.get(k) or "")}
            for k, cf_id in cfg.get("custom_fields", {}).items()
        ],
    }
    req = urllib.request.Request(
        "https://services.leadconnectorhq.com/contacts/upsert",
        data=json.dumps({k: v for k, v in body.items() if v}).encode(),
        headers={"Authorization": f"Bearer {token}", "Version": "2021-07-28",
                 "Content-Type": "application/json"}, method="POST")
    with urllib.request.urlopen(req, timeout=15) as r:
        r.read()

@app.get("/meta-leads")
def verify(request: Request):
    q = request.query_params
    if q.get("hub.mode") == "subscribe" and q.get("hub.verify_token") == VERIFY_TOKEN:
        return Response(q.get("hub.challenge", ""), media_type="text/plain")
    raise HTTPException(403)

@app.post("/meta-leads")
async def receive(request: Request):
    raw = await request.body()
    sig = request.headers.get("X-Hub-Signature-256", "")
    expected = "sha256=" + hmac.new(APP_SECRET, raw, hashlib.sha256).hexdigest()
    if not hmac.compare_digest(sig, expected):
        raise HTTPException(403)
    seen = set(SEEN.read_text().split()) if SEEN.exists() else set()
    for entry in json.loads(raw).get("entry", []):
        for change in entry.get("changes", []):
            if change.get("field") != "leadgen":
                continue
            v = change["value"]; lid = str(v["leadgen_id"]); page = str(v.get("page_id", ""))
            if lid in seen:
                continue
            cfg = CONFIG.get(page)
            if not cfg:
                _log({"t": time.time(), "leadgen_id": lid, "page_id": page, "status": "no_config"})
                continue
            try:
                lead = fetch_lead(lid)
                push_to_ghl(lead, cfg)
                status = "pushed"
            except Exception as e:  # loga e segue — Meta reenvia em falha 5xx, aqui escolhemos não duplicar
                status = f"error:{e}"
            seen.add(lid); SEEN.write_text("\n".join(seen))
            _log({"t": time.time(), "leadgen_id": lid, "page_id": page, "form_id": v.get("form_id"), "status": status})
    return {"ok": True}

@app.get("/meta-leads/health")
def health():
    lines = LOG.read_text().splitlines() if LOG.exists() else []
    last = json.loads(lines[-1]) if lines else None
    return {"received_total": len(lines), "last_event_at": last and last["t"],
            "last_status": last and last["status"]}
```

`requirements.txt`: `fastapi`, `uvicorn`. `config.example.json` com um cliente de exemplo e comentário no README sobre `custom_fields`.

- [ ] **Step 4: Rodar e ver passar.** `.venv/bin/pytest -q` → Expected: 6 passed.

- [ ] **Step 5: Commit.** `git add webhook-receiver/ && git commit -m "feat(receiver): webhook leadgen -> GHL com verificação de assinatura, dedup e health"`

### Task 10: Deploy do receiver na VPS + subscrição do app

**Files:**
- Create: `webhook-receiver/deploy/meta-leads.service` (systemd unit)
- Create: `webhook-receiver/deploy/RUNBOOK.md`

**Interfaces:**
- Consumes: spike T4 (APP_ID, App Secret, checklist), VPS Hermes (5.78.224.81, tunnel `ahoy-hermes` já publica webhooks.ahoy.digital).
- Produces: `https://webhooks.ahoy.digital/meta-leads` no ar; app subscrito com verify OK; página Ahoy com `subscribed_fields=leadgen`.

- [ ] **Step 1: Escrever a unit e o runbook.** `meta-leads.service`:

```ini
[Unit]
Description=Meta leadgen webhook receiver
After=network.target

[Service]
User=metaleads
WorkingDirectory=/opt/meta-leads
EnvironmentFile=/opt/meta-leads/.env
ExecStart=/opt/meta-leads/.venv/bin/uvicorn app:app --host 127.0.0.1 --port 8811
Restart=always

[Install]
WantedBy=multi-user.target
```

`RUNBOOK.md` com os passos exatos: criar user `metaleads`, `rsync` de `webhook-receiver/` pra `/opt/meta-leads`, venv + pip install, `.env` (META_VERIFY_TOKEN=`openssl rand -hex 24`, META_APP_SECRET, META_ACCESS_TOKEN, GHL_TOKEN_* por cliente), `config.json` real, systemd enable/start, e o ingress do tunnel: adicionar em `/etc/cloudflared/config.yml` a rota `webhooks.ahoy.digital` path `/meta-leads*` → `http://127.0.0.1:8811` (ANTES da rota catch-all existente) + `systemctl restart cloudflared`. **Atenção:** conferir se o Cloudflare Access cobre `webhooks.ahoy.digital` — a rota `/meta-leads*` precisa de bypass de Access (Service Auth/Bypass policy), senão a Meta não alcança o endpoint.

- [ ] **Step 2: Executar o runbook na VPS** (ssh root@5.78.224.81). Verificar: `curl "https://webhooks.ahoy.digital/meta-leads/health"` → JSON com `received_total`.

- [ ] **Step 3: Configurar o webhook no app Meta.** Painel do app (APP_ID do spike T4) → Webhooks → Page → Callback `https://webhooks.ahoy.digital/meta-leads`, verify token do `.env` → Verify and Save (a Meta chama o GET; deve passar) → subscrever campo `leadgen`. Depois subscrever a página Ahoy (comando do spike T4 Step 2).

- [ ] **Step 4: Teste ponta-a-ponta com lead de teste.** Gerar test lead (endpoint `test_leads` do spike T2 ou Lead Ads Testing Tool) num form `TEST_` da página Ahoy. Verificar: (1) `health` incrementou e `last_status=pushed`; (2) contato apareceu na subconta GHL de teste com source `meta-leadform-webhook`. Se `custom_fields` ainda não configurados, ok — Task 11 fecha isso.

- [ ] **Step 5: Commit.** `git add webhook-receiver/deploy/ && git commit -m "feat(receiver): deploy systemd + tunnel + runbook, e2e com test lead validado"`

### Task 11: Flow `crm` — integração nativa GHL (conexão, mapeamento, e2e)

**Files:**
- Create: `PLUGIN/flows/crm/SKILL.md`
- Create: `PLUGIN/commands/meta-ads-crm.md`
- Create: `PLUGIN/tests/19-crm-capi.sh`

**Interfaces:**
- Consumes: env novas `GHL_LOCATION_ID`, `GHL_PIT_TOKEN` (Private Integration Token da subconta — Settings → Private Integrations, scopes `contacts.readonly`+`contacts.write`); test lead do spike T2; receiver da Task 10.
- Produces: comando `/meta-ads-crm` com modos `status`, `mapear`, `testar`, `capi-setup`, `capi-testar` (os dois últimos na Task 12). Doctor (Task 21) reusa a função de check.

- [ ] **Step 1: Escrever `PLUGIN/commands/meta-ads-crm.md`** (thin wrapper, idioma de `commands/meta-ads-lead-forms.md`):

```markdown
---
description: "Pipeline de lead → GHL/FluxiHub: valida integração nativa, checklist de mapeamento de forms, teste ponta-a-ponta com test lead, setup e teste de CAPI do funil."
---

Invoque a skill `meta-ads-pro/crm` seguindo `flows/crm/SKILL.md`.

**Modos:**
- `status` → saúde da integração (GHL alcançável, receiver up, página subscrita)
- `mapear {form_id}` → checklist de mapeamento do form no GHL
- `testar {form_id}` → test lead → verifica contato na subconta
- `capi-setup` / `capi-testar` → Conversions API do funil (dataset + test event)

**Regras invioláveis:** nunca ecoar `GHL_PIT_TOKEN` nem `META_ACCESS_TOKEN`; test lead só em form `TEST_` ou com confirmação explícita.
```

- [ ] **Step 2: Escrever `PLUGIN/flows/crm/SKILL.md`** com as seções:

```markdown
---
name: meta-ads-crm
description: Integração do pipeline de lead com GoHighLevel/FluxiHub — nativa GHL como transporte, webhook próprio como atribuição/backup, CAPI do funil via ação nativa do GHL. Valida mapeamento e testa ponta-a-ponta.
---

# meta-ads-crm

## Env necessárias (.env do projeto do cliente)
GHL_LOCATION_ID=...        # subconta do cliente
GHL_PIT_TOKEN=...          # Private Integration Token (contacts.readonly + contacts.write)
RECEIVER_HEALTH_URL=https://webhooks.ahoy.digital/meta-leads/health

## Helper de chamada GHL (inline — o plugin não tem lib GHL)
ghl_api() {  # ghl_api GET|POST <path> [body]
  local method="$1" path="$2" body="${3:-}"
  local args=(-sS -H "Authorization: Bearer ${GHL_PIT_TOKEN:?}" -H "Version: 2021-07-28")
  [[ "$method" == "POST" ]] && args+=(-X POST -H "Content-Type: application/json" -d "$body")
  curl "${args[@]}" "https://services.leadconnectorhq.com${path}"
}

## Modo `status`
1. GHL alcançável: ghl_api GET "/locations/${GHL_LOCATION_ID}" → .location.name
2. Receiver up: curl -sS "$RECEIVER_HEALTH_URL" → received_total (warn se erro)
3. Página subscrita: graph_api GET "${PAGE_ID}/subscribed_apps" → app com leadgen
Mostrar tabela ✓/⚠/✗ com fix pra cada falha.

## Modo `mapear {form_id}`
Checklist interativo (a nativa GHL é configurada NA UI do GHL, não via API):
1. GET do form (nome, perguntas) via graph_api
2. Instruir: subconta → Settings → Integrations → Facebook → Form Fields Mapping
   → localizar o form pelo nome → mapear cada pergunta → campo do contato
   (perguntas custom → custom fields; criar antes se preciso)
3. Custom fields de atribuição: garantir utm_source/utm_medium/utm_campaign
   (listar existentes: ghl_api GET "/locations/${GHL_LOCATION_ID}/customFields";
    anotar os IDs → atualizar config.json do receiver, seção custom_fields)
4. Lembrete: form criado DEPOIS da conexão pode exigir refresh da lista no GHL.

## Modo `testar {form_id}`
1. Dispara test lead (mecanismo validado no spike 2026-07-leadform-avancado.md)
2. Aguarda 30s; busca o contato: ghl_api GET "/contacts/?locationId=${GHL_LOCATION_ID}&query=<email do test lead>"
3. Reporta: chegou via nativa? campos mapeados? UTMs presentes (nativa e/ou receiver)?
4. Confere receiver: health incrementou? último status = pushed?
Veredito final: NATIVA OK / SÓ RECEIVER / NADA CHEGOU (com diagnóstico).
```

(Manter o texto acima como conteúdo real do SKILL.md, expandido com os padrões da casa: pre-flight via doctor `--silent`, nunca ecoar tokens, tabelas PT-BR.)

- [ ] **Step 3: Teste automatizado.** `PLUGIN/tests/19-crm-capi.sh` (novo, mesmo harness `_pass/_fail/_skip`): test_01 `ghl_api GET /locations/{id}` retorna `.location.id == GHL_LOCATION_ID` (skip sem `GHL_PIT_TOKEN`); test_02 receiver health responde JSON com `received_total` (skip se `RECEIVER_HEALTH_URL` vazio); test_03 `subscribed_apps` contém `leadgen` (skip sem token/page). Rodar: `bash PLUGIN/tests/19-crm-capi.sh` → Expected: PASS/SKIP conforme env, 0 FAIL.

- [ ] **Step 4: Validação manual guiada.** Rodar `/meta-ads-crm mapear` + `testar` contra a subconta GHL de teste com um form `TEST_`. Expected: veredito NATIVA OK (ou diagnóstico claro). Registrar no spike T2 o que a nativa mapeou dos tracking_parameters (isso fecha a pergunta aberta do grill).

- [ ] **Step 5: Commit.** `git add PLUGIN/flows/crm/ PLUGIN/commands/meta-ads-crm.md PLUGIN/tests/19-crm-capi.sh && git commit -m "feat(crm): flow de integração GHL nativa com status/mapear/testar"`

### Task 12: CAPI do funil — setup, test event e verificação

**Files:**
- Modify: `PLUGIN/flows/crm/SKILL.md` (seções `capi-setup` e `capi-testar`)
- Modify: `PLUGIN/tests/19-crm-capi.sh` (testes 04–05)

**Interfaces:**
- Consumes: `PIXEL_ID` do CLAUDE.md do projeto; workflows do GHL (ação nativa "Facebook Conversions API").
- Produces: doctor check `check_capi_dataset` (Task 21 pluga no preflight).

- [ ] **Step 1: Teste primeiro.** Em `19-crm-capi.sh`:

```bash
# ─── Test 04: envio de test event CAPI (live, não polui otimização) ──────────
test_04_capi_test_event() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${PIXEL_ID:-}" || -z "${CAPI_TEST_EVENT_CODE:-}" ]]; then
    _skip "test_04_capi_test_event" "sem PIXEL_ID/CAPI_TEST_EVENT_CODE"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local em ph payload response
  em=$(echo -n "teste@ahoy.digital" | shasum -a 256 | awk '{print $1}')
  ph=$(echo -n "+5591999999999" | shasum -a 256 | awk '{print $1}')
  payload=$(jq -nc --arg em "$em" --arg ph "$ph" --arg tec "$CAPI_TEST_EVENT_CODE" \
    --argjson now "$(date +%s)" '{
    data: [{event_name:"LeadQualificado", event_time:$now, action_source:"system_generated",
            user_data:{em:[$em], ph:[$ph]},
            custom_data:{lead_event_source:"GHL", event_source:"crm"}}],
    test_event_code: $tec}')
  response=$(graph_api POST "${PIXEL_ID}/events" "$payload") \
    || _fail "test_04_capi_test_event" "POST falhou: $response"
  [[ "$(echo "$response" | jq -r '.events_received // 0')" == "1" ]] \
    || _fail "test_04_capi_test_event" "events_received != 1: $response"
  _pass "test_04_capi_test_event"
}
```

Rodar → Expected: SKIP (sem `CAPI_TEST_EVENT_CODE` ainda) — vira PASS no Step 3.

- [ ] **Step 2: Escrever as seções no SKILL.md:**

```markdown
## Modo `capi-setup`
1. Confirma PIXEL_ID no CLAUDE.md (pixel = dataset). Sem pixel → oferecer criação
   (flows/publicos, seção pixel — Task 19) ou Events Manager.
2. Guia a ação nativa no GHL (a fonte dos eventos é o WORKFLOW do GHL, não este plugin):
   workflow por marco do funil (qualificou / agendou / compareceu / fechou) →
   ação "Facebook Conversions API" → selecionar pixel + evento custom com o MESMO nome.
   Convenção de nomes: LeadQualificado, AgendamentoMarcado, Compareceu, Fechou.
3. Instrui pegar o test_event_code: Events Manager → aba Test Events do pixel.
   Salvar no .env: CAPI_TEST_EVENT_CODE=TESTxxxxx (só usado em teste).

## Modo `capi-testar`
1. Envia test event direto (payload do test_04 acima) → events_received==1
2. Instrui verificação visual: Events Manager → Test Events → evento na lista
3. Match quality: GET "${PIXEL_ID}?fields=name,last_fired_time" + orientar leitura
   do Event Match Quality no Events Manager (API não expõe EMQ — verificação manual).
4. Checklist final: workflows GHL publicados? eventos chegando SEM test_event_code
   depois do go-live? campanha otimizando pra evento custom (conversion_leads)?
```

- [ ] **Step 3: Validar live.** Pegar test_event_code do pixel Ahoy, exportar, rodar `bash PLUGIN/tests/19-crm-capi.sh` → Expected: test_04 PASS; evento visível no Test Events do Events Manager (confirmar no browser).

- [ ] **Step 4: Commit.** `git add PLUGIN/flows/crm/SKILL.md PLUGIN/tests/19-crm-capi.sh && git commit -m "feat(crm): CAPI do funil — setup guiado GHL + test events validados"`

---

## TRACK B — CRIATIVO (worktree `feat/v1.1-track-b`)

### Task 13: Flow `boost` — patrocinar post/Reel próprio do IG

**Files:**
- Create: `PLUGIN/flows/boost/SKILL.md`
- Create: `PLUGIN/commands/meta-ads-boost.md`
- Create: `PLUGIN/tests/18-boost-ig.sh`
- Modify: `PLUGIN/flows/orquestradora/SKILL.md` (roteamento de intenção: "impulsionar/boost/patrocinar post" → boost)

**Interfaces:**
- Consumes: payload validado no spike T3 (`source_instagram_media_id` + identidades); `INSTAGRAM_USER_ID` do CLAUDE.md; campanha/adset via flows existentes.
- Produces: comando `/meta-ads-boost` que lista mídia e cria ad PAUSED a partir dela.

- [ ] **Step 1: Teste live primeiro.** `PLUGIN/tests/18-boost-ig.sh` (harness padrão + cleanup trap de creative/ad):

```bash
# test_01: listagem de mídia retorna itens com media_product_type
test_01_list_ig_media() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${INSTAGRAM_USER_ID:-}" ]]; then
    _skip "test_01_list_ig_media" "sem INSTAGRAM_USER_ID"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local r; r=$(graph_api GET "${INSTAGRAM_USER_ID}/media?fields=id,media_type,media_product_type,permalink&limit=5") \
    || _fail "test_01_list_ig_media" "GET falhou"
  (( $(echo "$r" | jq '.data | length') >= 1 )) || _fail "test_01_list_ig_media" "sem mídia: $r"
  _pass "test_01_list_ig_media"
}

# test_02: creative com source_instagram_media_id (payload do spike T3) cria e deleta
test_02_creative_from_ig_media() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" || -z "${INSTAGRAM_USER_ID:-}" ]]; then
    _skip "test_02_creative_from_ig_media" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local media payload r cid
  media=$(graph_api GET "${INSTAGRAM_USER_ID}/media?fields=id,media_product_type&limit=10" \
    | jq -r '.data[] | select(.media_product_type=="FEED") | .id' | head -1)
  [[ -n "$media" ]] || { _skip "test_02_creative_from_ig_media" "sem post FEED"; return 0; }
  payload=$(jq -nc --arg pid "$PAGE_ID" --arg ig "$INSTAGRAM_USER_ID" --arg m "$media" \
    '{name:"TEST_boost_creative", object_story_spec:{page_id:$pid, instagram_user_id:$ig},
      source_instagram_media_id:$m}')   # ← forma final: docs/spikes/2026-07-boost-ig.md
  r=$(graph_api POST "${AD_ACCOUNT_ID}/adcreatives" "$payload") \
    || _fail "test_02_creative_from_ig_media" "POST falhou: $r"
  cid=$(echo "$r" | jq -r .id); created_creatives+=("$cid")
  _pass "test_02_creative_from_ig_media (cid=$cid)"
}
```

Rodar → Expected: PASS (o spike T3 já provou o caminho; se FAIL, o spike doc está desatualizado — corrigir lá primeiro).

- [ ] **Step 2: Escrever `PLUGIN/flows/boost/SKILL.md`** (fluxo de 6 passos, idioma da casa):

```markdown
---
name: meta-ads-boost
description: Impulsionar post ou Reel JÁ PUBLICADO do Instagram como anúncio — preserva curtidas/comentários (social proof), sem dark post. Lista a mídia da conta, valida elegibilidade e cria creative+ad PAUSED via campanha/conjunto existentes ou novos.
---

# meta-ads-boost

## Passo 1 — Pre-flight
Doctor --silent + exige INSTAGRAM_USER_ID no CLAUDE.md (se ausente: rodar /meta-ads-setup).

## Passo 2 — Listar mídia
graph_api GET "${INSTAGRAM_USER_ID}/media?fields=id,caption,media_type,media_product_type,permalink,timestamp,like_count,comments_count&limit=25"
Tabela: # | Data | Tipo (FEED/REELS) | Curtidas | Comentários | Caption (60c) | Link
Paginação via cursors se o user pedir mais.

## Passo 3 — Escolha + elegibilidade
User escolhe 1..N. Pra cada: avisar limitações do spike 2026-07-boost-ig.md
(ex.: Reel com música licenciada → erro <code do spike>; mídia de colab; idade).
Se o POST do creative falhar por elegibilidade, mostrar a explicação amigável e
voltar pra lista (não abortar o fluxo inteiro).

## Passo 4 — Campanha/conjunto
[1] Usar adset existente (lista via /meta-ads-campanha list) — default
[2] Criar novos (encadeia flows/campanha → flows/conjuntos, objetivo ENGAGEMENT
    ou LEADS conforme intenção declarada)

## Passo 5 — Creative + Ad (PAUSED)
payload=$(jq -nc --arg pid "$PAGE_ID" --arg ig "$INSTAGRAM_USER_ID" --arg m "$media_id" \
  --arg name "$(gen_name ...)" \
  '{name:$name, object_story_spec:{page_id:$pid, instagram_user_id:$ig},
    source_instagram_media_id:$m}')
creative_id=$(graph_api POST "${AD_ACCOUNT_ID}/adcreatives" "$payload" | jq -r .id)
ad: {name, adset_id, creative:{creative_id}, status:"PAUSED"}
Manifest: python3 lib/_py/manifest.py add creative/ad ... (rollback padrão).
NÃO aplicar UTM dinâmico: o destino é o próprio post (sem link externo). Se o user
anexar CTA com link externo (se o spike T3 mostrou que dá), aí sim build_utm_url_dynamic.

## Passo 6 — Resumo + preview + ativação
Mesmo padrão de flows/anuncios Passos 11-12 (link Ads Manager, ativar só com "s").
```

- [ ] **Step 3: Command wrapper + roteamento.** `commands/meta-ads-boost.md` (thin wrapper citando libs `graph_api.sh`, `nomenclatura.sh`, `manifest.py`, `rollback.sh`). Na orquestradora, adicionar à tabela de intenção: "impulsionar / boost / patrocinar post / turbinar publicação" → `flows/boost`.

- [ ] **Step 4: Validar fluxo completo live.** Rodar `/meta-ads-boost` no projeto Ahoy: escolher 1 post FEED real, criar em adset `TEST_`, conferir preview no Ads Manager (post com social proof), rollback no final. Rodar `bash PLUGIN/tests/18-boost-ig.sh` → 0 FAIL.

- [ ] **Step 5: Commit.** `git add PLUGIN/flows/boost/ PLUGIN/commands/meta-ads-boost.md PLUGIN/tests/18-boost-ig.sh PLUGIN/flows/orquestradora/SKILL.md && git commit -m "feat(boost): impulsionar post/Reel próprio do IG sem dark post"`

### Task 14: Preview oficial da Meta (`generatepreviews`)

**Files:**
- Modify: `PLUGIN/lib/visual-preview.sh` (nova função `preview_meta_oficial`)
- Modify: `PLUGIN/flows/anuncios/SKILL.md:211-229` (Passo 8) e `flows/boost/SKILL.md` (Passo 6)
- Modify: `PLUGIN/tests/20-criativo-avancado.sh` (criar arquivo nesta task)

**Interfaces:**
- Produces: `preview_meta_oficial <creative_spec_json|creative_id> <ad_format>` → salva HTML com os iframes oficiais em `~/.claude/meta-ads-pro/previews/` e abre no browser. Formatos: `MOBILE_FEED_STANDARD`, `INSTAGRAM_STANDARD`, `INSTAGRAM_STORY`, `INSTAGRAM_REELS`.

- [ ] **Step 1: Teste live.** Criar `PLUGIN/tests/20-criativo-avancado.sh` (harness padrão) com:

```bash
# test_01: generatepreviews retorna iframe pra um creative spec mínimo
test_01_generatepreviews() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" || -z "${PAGE_ID:-}" ]]; then
    _skip "test_01_generatepreviews" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local spec r
  spec=$(jq -nc --arg pid "$PAGE_ID" \
    '{object_story_spec:{page_id:$pid, link_data:{message:"preview test", link:"https://ahoy.digital"}}}')
  r=$(graph_api GET "${AD_ACCOUNT_ID}/generatepreviews?creative=$(python3 -c "import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1]))" "$spec")&ad_format=MOBILE_FEED_STANDARD") \
    || _fail "test_01_generatepreviews" "GET falhou: $r"
  echo "$r" | jq -re '.data[0].body' | grep -q "iframe" \
    || _fail "test_01_generatepreviews" "sem iframe no body: $r"
  _pass "test_01_generatepreviews"
}
```

Rodar → Expected: PASS direto (endpoint é GET puro; se falhar, checar formato na doc VNEW e ajustar).

- [ ] **Step 2: Implementar `preview_meta_oficial` em `lib/visual-preview.sh`:** monta a URL como no teste, aceita lista de `ad_format` (loop), concatena os `data[].body` num HTML único (`<h2>` por formato), grava em `~/.claude/meta-ads-pro/previews/preview_$(date +%s).html`, abre com `open`/`xdg-open`. Para creative já criado, usar `graph_api GET "{creative_id}/previews?ad_format=..."`.

- [ ] **Step 3: Plugar nos flows.** Em `flows/anuncios/SKILL.md` Passo 8: nova opção `[o] preview oficial Meta` ao lado do ASCII/HTML local (deixar claro: local = rápido/offline; oficial = fiel, requer API). Em `flows/boost/SKILL.md` Passo 6: preview oficial é o default (post real renderizado).

- [ ] **Step 4: Rodar + commit.** `bash PLUGIN/tests/20-criativo-avancado.sh` → 0 FAIL. `git add PLUGIN/lib/visual-preview.sh PLUGIN/flows/anuncios/SKILL.md PLUGIN/flows/boost/SKILL.md PLUGIN/tests/20-criativo-avancado.sh && git commit -m "feat(anuncios): preview oficial via generatepreviews"`

### Task 15: `image_crops` + asset customization por placement

**Files:**
- Modify: `PLUGIN/flows/anuncios/SKILL.md` (Passo 5 — detecção de dims; Passo 10 — payloads)
- Modify: `PLUGIN/tests/20-criativo-avancado.sh` (testes 02–03)

**Interfaces:**
- Consumes: `_detect_image_dims` (lib/upload_media.sh), modo Dinâmico (`asset_feed_spec`) existente.
- Produces: (a) crop 1:1 explícito via `image_crops` no creative Normal; (b) imagem certa por placement via `asset_customization_rules` no Dinâmico.

- [ ] **Step 1: Testes live** em `20-criativo-avancado.sh`:

```bash
# ─── Test 02: creative Normal com image_crops (crop 1:1 centrado de img 1200x800)
test_02_image_crops() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" || -z "${PAGE_ID:-}" ]]; then
    _skip "test_02_image_crops" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"; source "$PLUGIN_ROOT/lib/upload_media.sh"
  local fixture hash payload r cid
  fixture=$(ls "$PLUGIN_ROOT"/tests/fixtures/*.jpg 2>/dev/null | head -1)
  [[ -n "$fixture" ]] || { _skip "test_02_image_crops" "sem fixture jpg"; return 0; }
  hash=$(upload_image "$fixture") || _fail "test_02_image_crops" "upload falhou"
  payload=$(jq -nc --arg pid "$PAGE_ID" --arg h "$hash" '{
    name:"TEST_crops",
    object_story_spec:{page_id:$pid,
      link_data:{message:"crop test", link:"https://ahoy.digital", image_hash:$h}},
    image_crops:{"100x100":[[200,0],[1000,800]]}}')
  r=$(graph_api POST "${AD_ACCOUNT_ID}/adcreatives" "$payload") \
    || _fail "test_02_image_crops" "POST falhou: $r"
  cid=$(echo "$r" | jq -r .id); created_creatives+=("$cid")
  graph_api GET "${cid}?fields=image_crops" | jq -e '.image_crops["100x100"]' >/dev/null \
    || _fail "test_02_image_crops" "image_crops não persistiu"
  _pass "test_02_image_crops (cid=$cid)"
}

# ─── Test 03: asset_feed_spec com asset_customization_rules (feed vs story)
test_03_asset_customization_rules() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" || -z "${PAGE_ID:-}" ]]; then
    _skip "test_03_asset_customization_rules" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"; source "$PLUGIN_ROOT/lib/upload_media.sh"
  local f1 f2 h1 h2 payload r cid
  f1=$(ls "$PLUGIN_ROOT"/tests/fixtures/*1080x1080*.jpg 2>/dev/null | head -1)
  f2=$(ls "$PLUGIN_ROOT"/tests/fixtures/*1080x1920*.jpg 2>/dev/null | head -1)
  # se fixtures dedicadas não existem, gerar via sips a partir de qualquer jpg (macOS)
  [[ -n "$f1" && -n "$f2" ]] || { _skip "test_03_asset_customization_rules" "sem fixtures 1080"; return 0; }
  h1=$(upload_image "$f1"); h2=$(upload_image "$f2")
  payload=$(jq -nc --arg pid "$PAGE_ID" --arg h1 "$h1" --arg h2 "$h2" '{
    name:"TEST_asset_custom",
    object_story_spec:{page_id:$pid},
    asset_feed_spec:{
      images:[{hash:$h1, adlabels:[{name:"img_feed"}]},
              {hash:$h2, adlabels:[{name:"img_story"}]}],
      bodies:[{text:"corpo"}], titles:[{text:"titulo"}],
      link_urls:[{website_url:"https://ahoy.digital"}],
      call_to_action_types:["LEARN_MORE"],
      ad_formats:["SINGLE_IMAGE"],
      asset_customization_rules:[
        {customization_spec:{publisher_platforms:["facebook","instagram"],
           facebook_positions:["feed"], instagram_positions:["stream"]},
         image_label:{name:"img_feed"}},
        {customization_spec:{publisher_platforms:["facebook","instagram"],
           facebook_positions:["story"], instagram_positions:["story"]},
         image_label:{name:"img_story"}}]}}')
  r=$(graph_api POST "${AD_ACCOUNT_ID}/adcreatives" "$payload") \
    || _fail "test_03_asset_customization_rules" "POST falhou: $r"
  cid=$(echo "$r" | jq -r .id); created_creatives+=("$cid")
  _pass "test_03_asset_customization_rules (cid=$cid)"
}
```

(Arquivo usa o mesmo trap de cleanup da Task 13: array `created_creatives` + DELETE no EXIT.) Rodar → ajustar sintaxe conforme erro da API (documentar qualquer divergência num apêndice do spike da Task 1).

- [ ] **Step 2: Escrever as seções no `flows/anuncios/SKILL.md`.** No Passo 5, após a detecção de dims: se a imagem não é 1:1/9:16 nativa, oferecer `[c] crop explícito` (calcula janela centrada pro aspect alvo e monta `image_crops`) além do resize atual. No Passo 10 (Dinâmico): nova subseção "Placement customization" — pergunta "Imagens diferentes por posicionamento? [s/N]"; se sim, coleta pares imagem↔grupo de placements e monta `asset_customization_rules` (payload completo do test_03 como referência no doc). Regra: toda imagem referenciada em rule precisa do `adlabel` correspondente.

- [ ] **Step 3: Rodar + commit.** `bash PLUGIN/tests/20-criativo-avancado.sh` → 0 FAIL. `git add PLUGIN/flows/anuncios/SKILL.md PLUGIN/tests/20-criativo-avancado.sh && git commit -m "feat(anuncios): image_crops e asset_customization_rules por placement"`

### Task 16: Carrossel (child_attachments)

**Files:**
- Modify: `PLUGIN/flows/anuncios/SKILL.md` (Passo 2 ganha opção; nova seção de payload)
- Modify: `PLUGIN/tests/20-criativo-avancado.sh` (teste 04)

**Interfaces:**
- Consumes: `upload_image` (hashes), `build_utm_url_dynamic` (link de cada cartão).

- [ ] **Step 1: Teste live** (`test_04_carousel`): upload de 3 fixtures → payload:

```json
{"name":"TEST_carousel","object_story_spec":{"page_id":"...","link_data":{
  "message":"Legenda do carrossel","link":"https://ahoy.digital?utm_...",
  "child_attachments":[
    {"link":"https://ahoy.digital/a?utm_...","image_hash":"h1","name":"Card 1","description":"Desc 1"},
    {"link":"https://ahoy.digital/b?utm_...","image_hash":"h2","name":"Card 2","description":"Desc 2"},
    {"link":"https://ahoy.digital/c?utm_...","image_hash":"h3","name":"Card 3","description":"Desc 3"}],
  "multi_share_optimized":true,"multi_share_end_card":false}}}
```

POST adcreatives → id; cleanup. Rodar → Expected: PASS.

- [ ] **Step 2: SKILL.md.** Passo 2 vira menu de 3: `[1] Normal  [2] Dinâmico  [3] Carrossel (2-10 cartões)`. Nova seção "Modo Carrossel": coleta N imagens (2–10) + copy por cartão (reusa pipeline de copy com 1 headline/desc por cartão), UTM dinâmico em CADA `child_attachments[].link` e no `link` principal (regra inviolável 8 se aplica por cartão), preview oficial (Task 14) recomendado, criação PAUSED + manifest.

- [ ] **Step 3: Rodar + commit.** `git add PLUGIN/flows/anuncios/SKILL.md PLUGIN/tests/20-criativo-avancado.sh && git commit -m "feat(anuncios): modo carrossel com child_attachments e UTM por cartão"`

---

## TRACK C — COMPLETUDE (worktree `feat/v1.1-track-c`)

### Task 17: Exclusões de público no targeting

**Files:**
- Modify: `PLUGIN/flows/conjuntos/SKILL.md:177-206` (Passo 6) e a seção de payload
- Modify: `PLUGIN/flows/publicos/SKILL.md` (nota de uso em exclusão)
- Modify: `PLUGIN/tests/06-conjuntos-targeting.sh` (novo teste)

**Interfaces:**
- Produces: targeting com `excluded_custom_audiences` e `exclusions` (flexible_spec negativo). Caso de uso âncora: excluir `leads-crm-180d` (já é paciente) das campanhas de captação.

- [ ] **Step 1: Teste live** em `06-conjuntos-targeting.sh` (seguir o idioma dos testes existentes do arquivo — criar campanha `TEST_` + adset com exclusão e verificar round-trip):

```bash
# ─── Test: adset com excluded_custom_audiences persiste a exclusão ────────────
test_excluded_custom_audience() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" ]]; then
    _skip "test_excluded_custom_audience" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local aud camp adset r
  aud=$(graph_api POST "${AD_ACCOUNT_ID}/customaudiences" "$(jq -nc '{
    name:"TEST_excl", subtype:"CUSTOM", description:"teste exclusão",
    customer_file_source:"USER_PROVIDED_ONLY"}')" | jq -r .id)
  created_audiences+=("$aud")
  camp=$(graph_api POST "${AD_ACCOUNT_ID}/campaigns" "$(jq -nc '{
    name:"TEST_excl_camp", objective:"OUTCOME_LEADS", status:"PAUSED",
    special_ad_categories:[], is_adset_budget_sharing_enabled:false}')" | jq -r .id)
  created_campaigns+=("$camp")
  adset=$(graph_api POST "${AD_ACCOUNT_ID}/adsets" "$(jq -nc --arg c "$camp" --arg a "$aud" '{
    name:"TEST_excl_adset", campaign_id:$c, status:"PAUSED",
    daily_budget:"1000", billing_event:"IMPRESSIONS", optimization_goal:"LEAD_GENERATION",
    destination_type:"ON_AD", promoted_object:null,
    targeting:{geo_locations:{countries:["BR"]},
      excluded_custom_audiences:[{id:$a}],
      targeting_automation:{advantage_audience:0}}}' \
    | jq 'del(.promoted_object)')" ) || _fail "test_excluded_custom_audience" "adset falhou: $adset"
  adset=$(echo "$adset" | jq -r .id); created_adsets+=("$adset")
  r=$(graph_api GET "${adset}?fields=targeting")
  [[ "$(echo "$r" | jq -r '.targeting.excluded_custom_audiences[0].id')" == "$aud" ]] \
    || _fail "test_excluded_custom_audience" "exclusão não persistiu: $r"
  _pass "test_excluded_custom_audience"
}
```

Ajustar os campos mínimos do adset ao padrão que os testes existentes de `06-conjuntos-targeting.sh` já usam pra LEADS (copiar o builder local do arquivo, que já resolve `promoted_object`/`destination_type` corretos). Cleanup de adset+campanha+audience nos traps existentes do arquivo. Rodar → Expected: PASS.

- [ ] **Step 2: SKILL.md conjuntos.** No Passo 6, após a escolha do público, nova pergunta padrão:

```markdown
**Exclusões (pergunta SEMPRE, default nenhuma):**
Excluir algum público? (ex.: clientes atuais, leads já convertidos) [n/lista]
[1] leads/pacientes do CRM   [2] compradores/fechados   [3] escolher da lista   [n] nenhum

Payload: targeting.excluded_custom_audiences: [{id:"AUD_ID"}]
Exclusão por interesse/comportamento (raro): targeting.exclusions: {interests:[{id,name}]}
Regra: exclusão NUNCA substitui advantage_audience — os dois coexistem no payload
(advantage_audience expande inclusões; exclusões são respeitadas mesmo expandindo).
```

Em `flows/publicos/SKILL.md`, na seção 3.3 (Serviços), adicionar linha na tabela: público `leads-crm-180d` marcado "usar também como EXCLUSÃO nas campanhas de captação".

- [ ] **Step 3: Rodar + commit.** `bash PLUGIN/tests/06-conjuntos-targeting.sh` → 0 FAIL. `git add PLUGIN/flows/conjuntos/SKILL.md PLUGIN/flows/publicos/SKILL.md PLUGIN/tests/06-conjuntos-targeting.sh && git commit -m "feat(conjuntos): exclusões de público (excluded_custom_audiences + exclusions)"`

### Task 18: Saved audiences — escrita

**Files:**
- Modify: `PLUGIN/flows/publicos/SKILL.md` (seção 1 tabela + nova seção 2.6)
- Modify: `PLUGIN/tests/09-publicos.sh` (novo teste)

- [ ] **Step 1: Teste live** em `09-publicos.sh`:

```bash
# ─── Test: criar saved audience e conferir round-trip ─────────────────────────
test_saved_audience_create() {
  if [[ -z "${META_ACCESS_TOKEN:-}" || -z "${AD_ACCOUNT_ID:-}" ]]; then
    _skip "test_saved_audience_create" "sem env"; return 0
  fi
  source "$PLUGIN_ROOT/lib/graph_api.sh"
  local payload r sid
  payload=$(jq -nc '{name:"TEST_saved_br_25-45",
    targeting:{geo_locations:{countries:["BR"]}, age_min:25, age_max:45,
               targeting_automation:{advantage_audience:0}}}')
  r=$(GRAPH_API_SKIP_RESOLVER=1 graph_api POST "${AD_ACCOUNT_ID}/saved_audiences" "$payload" 2>&1) \
    || _fail "test_saved_audience_create" "POST falhou (se a VNEW não expõe escrita, ver nota da task): $r"
  sid=$(echo "$r" | jq -r .id); created_saved_audiences+=("$sid")
  [[ "$(graph_api GET "${sid}?fields=name" | jq -r .name)" == "TEST_saved_br_25-45" ]] \
    || _fail "test_saved_audience_create" "round-trip falhou"
  _pass "test_saved_audience_create (sid=$sid)"
}
```

Cleanup: `graph_api DELETE "$sid"` no trap. Rodar → Expected: PASS. Se a VNEW rejeitar o endpoint de escrita, registrar no spike da Task 1 e transformar a seção 2.6 em "somente leitura + criação guiada no Ads Manager" — decisão documentada, não silêncio (o teste vira guard-rail no estilo do test_11 da Task 8).

- [ ] **Step 2: SKILL.md publicos.** Tabela da seção 1 ganha linha `Criar saved audience | act_{id}/saved_audiences | POST`. Nova seção 2.6 com o fluxo: coleta geo/idade/gênero/interesses (mesmas perguntas do flows/conjuntos Passos 3–6, linkar em vez de duplicar), monta `targeting` completo (com `targeting_automation.advantage_audience` SEMPRE presente — mesma regra do bug #2), POST, confirmação padrão da seção 10.

- [ ] **Step 3: Rodar + commit.** `git add PLUGIN/flows/publicos/SKILL.md PLUGIN/tests/09-publicos.sh && git commit -m "feat(publicos): criação de saved audiences via API"`

### Task 19: Criação de pixel via API

**Files:**
- Modify: `PLUGIN/flows/publicos/SKILL.md` (nova seção "Pixel")
- Modify: `PLUGIN/flows/setup/SKILL.md:117-134` (Passo 8: oferecer criação se 0 pixels)
- Modify: `PLUGIN/tests/09-publicos.sh` (teste)

- [ ] **Step 1: Teste live** (`test_XX_pixel_create`): `POST act_X/adspixels {"name":"TEST_pixel_$(date +%s)"}` → id retorna; `GET act_X/adspixels?fields=name` contém o nome. **Sem cleanup** — pixel não tem DELETE na Graph API; usar nome `TEST_` e anotar no teste que a conta de teste acumula (aceitável: 1 pixel por rodada é evitado com guard `SKIP_PIXEL_CREATE=1` default; o teste só roda com `RUN_PIXEL_CREATE=1` explícito). Rodar com a flag → Expected: PASS.

- [ ] **Step 2: SKILL.md.** Seção "Pixel" em publicos: criar (`POST act_{id}/adspixels {name}`), listar código de instalação (`GET {pixel_id}?fields=code`), verificar disparo (`GET {pixel_id}?fields=last_fired_time` + orientar Test Events). No setup Passo 8: se `adspixels` retornar 0, perguntar "Criar pixel agora? [s/N]" → POST + salvar `pixel_id` no CLAUDE.md (hoje o campo fica comentado — `flows/setup/SKILL.md:186`).

- [ ] **Step 3: Rodar + commit.** `git add PLUGIN/flows/publicos/SKILL.md PLUGIN/flows/setup/SKILL.md PLUGIN/tests/09-publicos.sh && git commit -m "feat(publicos): criação de pixel via API + oferta no setup"`

### Task 20: Comando `/meta-ads-news`

**Files:**
- Create: `PLUGIN/flows/news/SKILL.md`
- Create: `PLUGIN/commands/meta-ads-news.md`

**Interfaces:**
- Consumes: `META_API_VERSION` efetiva (`lib/graph_api.sh:9`), changelog oficial (WebFetch).
- Produces: relatório sob demanda "o que mudou na API e o que afeta este plugin".

- [ ] **Step 1: Command wrapper** (`commands/meta-ads-news.md`):

```markdown
---
description: "Novidades da Marketing API: compara a versão do plugin com a mais recente, lista breaking changes/deprecações que afetam os flows e sugere ações. Sob demanda, sem infra."
---

Invoque a skill `meta-ads-pro/news` seguindo `flows/news/SKILL.md`.
```

- [ ] **Step 2: Flow** (`flows/news/SKILL.md`):

```markdown
---
name: meta-ads-news
description: Checagem sob demanda do changelog da Marketing API contra a versão usada pelo plugin. Reporta breaking changes, deprecações com data e features novas relevantes pro funil de leads.
---

# meta-ads-news

## Passo 1 — Versão em uso
grep 'META_API_VERSION' no .env do projeto; fallback: default em lib/graph_api.sh.

## Passo 2 — Buscar o changelog oficial
WebFetch https://developers.facebook.com/docs/graph-api/changelog
(+ página da versão mais nova, e https://developers.facebook.com/docs/marketing-api/changelog se divergir).

## Passo 3 — Cruzar com a superfície do plugin
Superfície (manter esta lista atualizada quando novo flow entrar):
leadgen_forms, test_leads, adcreatives (object_story_spec, asset_feed_spec,
source_instagram_media_id, image_crops, asset_customization_rules, child_attachments),
generatepreviews, customaudiences, saved_audiences, adspixels, {pixel}/events (CAPI),
adrules_library, insights, search (adinterest/adgeolocation), subscribed_apps, {ig_user}/media.

## Passo 4 — Relatório PT-BR
┌ Versão do plugin: vX · Mais recente: vY · Depreciação de vX: <data ou "sem data">
├ 🔴 Breaking (afeta flow Z): ...     → ação sugerida
├ 🟡 Deprecação futura: ...           → prazo
└ 🟢 Novidade aproveitável: ...       → ideia de uso no funil
Se nada afeta: dizer isso explicitamente (zero alarme falso).
NUNCA inventar item de changelog — só reportar o que a página buscada diz;
se o fetch falhar, reportar a falha e parar.
```

- [ ] **Step 3: Validar.** Rodar `/meta-ads-news` de verdade; conferir que o relatório cita a versão real e itens verificáveis na página. Commit: `git add PLUGIN/flows/news/ PLUGIN/commands/meta-ads-news.md && git commit -m "feat(news): checagem sob demanda do changelog da Marketing API"`

### Task 21: Setup + doctor cobrindo os recursos novos

**Files:**
- Modify: `PLUGIN/flows/setup/SKILL.md` (novos passos opcionais 8.5 e 11)
- Modify: `PLUGIN/lib/preflight.sh` (checks 11–14 + registro no runner `:223-231`)
- Modify: `PLUGIN/flows/doctor/SKILL.md` (documentar os checks novos)
- Modify: `PLUGIN/tests/04-doctor.sh` (asserts dos checks novos)

**Interfaces:**
- Consumes: env novas (`GHL_LOCATION_ID`, `GHL_PIT_TOKEN`, `RECEIVER_HEALTH_URL`, `CAPI_TEST_EVENT_CODE` opcional).
- Produces: doctor com 14 checks; setup grava as env novas e o bloco GHL no CLAUDE.md.

- [ ] **Step 1: Checks novos em `preflight.sh`** (código completo, mesmo padrão dos existentes — retornos 0/1/2):

```bash
# ─── check 11: GHL/FluxiHub conectável ─────────────────────────────────────
check_ghl() {
  [[ -z "${GHL_PIT_TOKEN:-}" || -z "${GHL_LOCATION_ID:-}" ]] \
    && { echo "⚠ GHL não configurado (ok se não usa CRM) — /meta-ads-crm"; return 1; }
  local r
  r=$(curl -sS -H "Authorization: Bearer ${GHL_PIT_TOKEN}" -H "Version: 2021-07-28" \
    "https://services.leadconnectorhq.com/locations/${GHL_LOCATION_ID}" 2>/dev/null || true)
  if echo "$r" | jq -e '.location.id' >/dev/null 2>&1; then
    echo "✓ GHL: subconta $(echo "$r" | jq -r .location.name)"
  else
    echo "✗ GHL token/location inválidos — regenerar Private Integration"; return 2
  fi
}

# ─── check 12: receiver de leadgen no ar ───────────────────────────────────
check_receiver() {
  [[ -z "${RECEIVER_HEALTH_URL:-}" ]] \
    && { echo "⚠ Receiver não configurado (webhook de atribuição off)"; return 1; }
  local r
  r=$(curl -sS --max-time 10 "$RECEIVER_HEALTH_URL" 2>/dev/null || true)
  if echo "$r" | jq -e '.received_total' >/dev/null 2>&1; then
    echo "✓ Receiver up ($(echo "$r" | jq -r .received_total) leads recebidos)"
  else
    echo "✗ Receiver fora do ar — leads seguem só pela nativa GHL"; return 2
  fi
}

# ─── check 13: página subscrita em leadgen ─────────────────────────────────
check_leadgen_subscription() {
  local page_id="${PAGE_ID:?}"
  local r
  r=$(graph_api GET "${page_id}/subscribed_apps?fields=subscribed_fields" 2>/dev/null || true)
  if echo "$r" | jq -e '.data[]?.subscribed_fields | index("leadgen")' >/dev/null 2>&1; then
    echo "✓ Página subscrita em leadgen"
  else
    echo "⚠ Página SEM subscrição leadgen — webhook não recebe (runbook Task 10)"; return 1
  fi
}

# ─── check 14: dataset CAPI ativo ──────────────────────────────────────────
check_capi_dataset() {
  [[ -z "${PIXEL_ID:-}" ]] && { echo "⚠ Sem PIXEL_ID — CAPI do funil inativa"; return 1; }
  local r last
  r=$(graph_api GET "${PIXEL_ID}?fields=name,last_fired_time" 2>/dev/null || true)
  last=$(echo "$r" | jq -r '.last_fired_time // empty')
  if [[ -n "$last" ]]; then
    echo "✓ CAPI/pixel: último evento em $last"
  else
    echo "⚠ Pixel sem eventos ainda — rodar /meta-ads-crm capi-testar"; return 1
  fi
}
```

Registrar os 4 no `run_preflight` após `check_learnings` (`preflight.sh:231`). `PIXEL_ID` passa a ser exportado pelo carregamento de env (documentar no setup).

- [ ] **Step 2: Setup.** Novo Passo 8.5 no `flows/setup/SKILL.md`: "Integra com GHL/FluxiHub? [s/N]" → coleta `GHL_LOCATION_ID` + `GHL_PIT_TOKEN` (instrução de gerar Private Integration na subconta) + `RECEIVER_HEALTH_URL` (default `https://webhooks.ahoy.digital/meta-leads/health`), grava no `.env` (mesmo padrão .gitignore-first do Passo 5). Passo 11: bloco CLAUDE.md ganha `ghl_location_id: ...` (token NUNCA no CLAUDE.md).

- [ ] **Step 3: Testes.** Em `tests/04-doctor.sh`, adicionar asserts: sem env GHL → check_ghl retorna 1 com "⚠"; com `RECEIVER_HEALTH_URL` apontando pra `https://example.com/404` → check_receiver retorna 2. Rodar `bash PLUGIN/tests/04-doctor.sh` → 0 FAIL.

- [ ] **Step 4: Commit.** `git add PLUGIN/lib/preflight.sh PLUGIN/flows/setup/SKILL.md PLUGIN/flows/doctor/SKILL.md PLUGIN/tests/04-doctor.sh && git commit -m "feat(doctor+setup): checks GHL, receiver, subscrição leadgen e CAPI (14 checks)"`

---

## F-FINAL (sequencial, após merge dos 3 tracks)

### Task 22: Merge dos tracks + suíte completa

- [ ] **Step 1:** Merge `feat/v1.1-track-a`, `-b`, `-c` em `feat/v1.1-operacao-ahoy` (resolver conflitos — esperados em `flows/anuncios/SKILL.md`, `preflight.sh` e `tests/`).
- [ ] **Step 2:** `bash PLUGIN/tests/run_all.sh` com env completa do projeto Ahoy → Expected: 0 FAIL em todas as suítes (00–20). `bash PLUGIN/tests/01-lint.sh` limpo (shellcheck nos novos trechos).
- [ ] **Step 3:** Commit de merge + push da branch (sem PR ainda).

### Task 23: Validação ponta-a-ponta na infra Ahoy

**Files:**
- Create: `docs/validacao/2026-07-e2e-infra-ahoy.md` (evidências)

- [ ] **Step 1: Cenário completo, tudo `TEST_` e PAUSED, no projeto Ahoy:** (1) `/meta-ads-doctor` → 14 checks, sem ✗; (2) campanha LEADS + conjunto com exclusão de público + lead form com tracking_parameters, thank you dupla com CTA WhatsApp; (3) test lead → contato na subconta GHL de teste (nativa) + receiver `last_status=pushed` + UTMs nos custom fields; (4) `/meta-ads-crm capi-testar` → evento no Test Events; (5) `/meta-ads-boost` com post real da Ahoy → ad PAUSED com social proof no preview oficial; (6) carrossel + asset customization → previews oficiais ok; (7) `/meta-ads-news` → relatório real.
- [ ] **Step 2: Registrar evidências** no doc (IDs, timestamps, prints/URLs de preview, output do doctor). Qualquer falha → issue na task correspondente antes de seguir.
- [ ] **Step 3: Rollback total** via `/meta-ads-rollback` do run + conferência no Ads Manager de que a conta Ahoy ficou limpa. Commit do doc.

### Task 24: Versão, docs e dívidas

**Files:**
- Modify: `PLUGIN/.claude-plugin/plugin.json` (1.0.6 → **1.1.0**; description ganha "boost IG, CRM/GHL, CAPI, tracking")
- Modify: `README.md` (banner v1.0.4 → v1.1.0 — corrige a divergência; seção de features novas; tabela de comandos + `/meta-ads-crm`, `/meta-ads-boost`, `/meta-ads-news`)
- Modify: `CHANGELOG.md` (entry 1.1.0 com tudo desta rodada; remover `_Nada pendente._` do Unreleased)
- Modify: `PLUGIN/flows/lead-forms/SKILL.md:308` (remover "(a ser criado em CP3c)" — script existe)
- Modify: `LEARNINGS.md` (novas lições da rodada: formato real de tracking_parameters, elegibilidade boost, formato GHL upsert, etc.)

- [ ] **Step 1:** Aplicar as edições acima. `grep -rn "v1.0.4\|CP3c\|v25.0" README.md PLUGIN/ | grep -v tests/reports` → só menções históricas intencionais (CHANGELOG).
- [ ] **Step 2:** Commit `chore: release v1.1.0 — pipeline de lead completo, boost IG, criativo avançado`. Propor PR pra `main` (pedir confirmação do user antes do push, regra da casa).

---

## Fase de piloto (fora deste plano)

Rollout num cliente real (subconta GHL própria, página/IG do cliente, subscrição da página no receiver + `config.json`) só depois do Task 23 verde. Vira um runbook curto derivado de `docs/validacao/2026-07-e2e-infra-ahoy.md`.



